
# con node instalado ejecutar el comando
npm install -g --unsafe-perm node-red
npm install -g node-red-dashboard

# iniciar el servidor abriendo consola y escribiendo:
node-red

http://localhost:1880

# en esta ruta puedo ver los botones de encender apagar mosquitto, telegraf
http://localhost:1880/ui/

# parar el servicio

Ctrl + C   (en la consola donde lo hemos arrancado)